function menuVal(){
	var menu = document.getElementsByName('menubox');
	var val ="";
	for(var i=0;i<menu.length;i++){
		if(menu[i].checked){
			val += menu[i].value+","
		}
	}
	document.getElementById('menu').value=val.substring(0,val.length-1);
}